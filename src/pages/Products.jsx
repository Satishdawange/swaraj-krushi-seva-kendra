import React,{useMemo,useState} from 'react';
import { Search, SlidersHorizontal, X } from '../components/Icons';
import { categories,products } from '../data/products';
import ProductCard from '../components/ProductCard';
export default function Products(){const [q,setQ]=useState('');const [cat,setCat]=useState('सर्व उत्पादने');const filtered=useMemo(()=>products.filter(p=>(cat==='सर्व उत्पादने'||p.category===cat)&&(`${p.name} ${p.company} ${p.category}`.toLowerCase()).includes(q.toLowerCase())),[q,cat]);return <section className="products-page"><div className="page-hero"><span className="pill">🌿 उत्पादन संग्रह</span><h1>शेतीसाठी उत्पादने</h1><p>उत्पादनाचे नाव, कंपनी किंवा श्रेणीने शोधा.</p></div><div className="catalog"><div className="search-wrap"><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="उत्पादन शोधा... उदा. मॅन्कोझेब"/>{q&&<button onClick={()=>setQ('')}><X/></button>}</div><div className="cat-row"><SlidersHorizontal size={18}/>{categories.map(c=><button key={c} className={cat===c?'active':''} onClick={()=>setCat(c)}>{c}</button>)}</div><div className="result-line"><span><b>{filtered.length}</b> उत्पादने सापडली</span></div>{filtered.length?<div className="product-grid">{filtered.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<div className="empty"><div>🌱</div><h3>उत्पादन सापडले नाही</h3><p>शोध शब्द बदलून पुन्हा प्रयत्न करा.</p></div>}</div></section>}
